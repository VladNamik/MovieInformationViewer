<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vladnamik.developer.movieinformationviewer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".components.Application_"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".components.activities.MainActivity_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".components.activities.MovieListActivity_" />
        <activity android:name=".components.activities.FullMovieInfoActivity_" />

        <service
            android:name=".components.services.APIService"
            android:exported="false" />
    </application>

</manifest>